#include<stdio.h>
int main(){

int n;
printf("enter the value of n: ");
scanf("%d",&n);
    int arr[n];
printf("enter the value = \n");
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    //printf("after reverse\n ");
    //for (int  i = 4; i >=0; i--)
    //{
     //   printf("%d\n",arr[i]);
  //  }
    //int sum = 0;
   // for (int i = 0; i < n; i++)
   // {
       // sum=sum+arr[i];
   // }
  //  printf("sum = %d", sum);

    
    return 0;

}